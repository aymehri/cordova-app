## Cordova App

to make project run:
```
$ npm install
$ cordova prepare
```

Usage:
```
$ cordova create myApp
$ cordova platform add browser --save
$ cordova platform add android --save
$ cordova run browser
$ cordova run android
$ 
$ 
```
